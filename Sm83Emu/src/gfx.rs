pub mod gfx {
    use winit::event_loop::{
        EventLoop
    };

    use winit::window::WindowBuilder

    pub fn drew_frame() -> Result<(), Error> {
        let event_loop = EventLoop::new();
        

    }
}

mod tests {
    use super::*;

    #[test]
    fn test1() {
        drew_frame();
    }
}
